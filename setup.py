from distutils.core import setup
setup(name='gd-lve-collector',
      version='0.1',
      py_modules=['gd-lve-collector', 'lveconfig', 'lvelog'],
      #      /etc/sysconfig/gd-lve-config.conf
     )
